<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- App Colors -->
    <Color x:Key="AccentPrimaryColor">#6366F1</Color>
    <Color x:Key="AccentSecondaryColor">#8B5CF6</Color>
    <Color x:Key="AccentTertiaryColor">#EC4899</Color>
    
    <!-- Animation Durations -->
    <Duration x:Key="FastAnimationDuration">0:0:0.15</Duration>
    <Duration x:Key="NormalAnimationDuration">0:0:0.25</Duration>
    <Duration x:Key="SlowAnimationDuration">0:0:0.4</Duration>
    
    <!-- Gradient Brushes -->
    <LinearGradientBrush x:Key="GradientBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#1a1a2e" Offset="0.0"/>
        <GradientStop Color="#16213e" Offset="0.5"/>
        <GradientStop Color="#0f3460" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="SubtleGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#20FFFFFF" Offset="0.0"/>
        <GradientStop Color="#05FFFFFF" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{StaticResource AccentPrimaryColor}" Offset="0.0"/>
        <GradientStop Color="{StaticResource AccentSecondaryColor}" Offset="1.0"/>
    </LinearGradientBrush>

    <!-- Theme Transitions for Smooth UI -->
    <TransitionCollection x:Key="EntranceTransitions">
        <EntranceThemeTransition IsStaggeringEnabled="True"/>
    </TransitionCollection>

    <TransitionCollection x:Key="ContentTransitions">
        <ContentThemeTransition/>
    </TransitionCollection>

    <TransitionCollection x:Key="RepositionTransitions">
        <RepositionThemeTransition/>
    </TransitionCollection>

    <TransitionCollection x:Key="PopupTransitions">
        <PopupThemeTransition/>
    </TransitionCollection>

    <!-- Card Style -->
    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12"/>
    </Style>

    <!-- Animated Card Style with Transitions -->
    <Style x:Key="AnimatedCardStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
        <Setter Property="Transitions">
            <Setter.Value>
                <TransitionCollection>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Icon Button Style with Smooth Transitions -->
    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Primary Action Button -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="16,8"/>
    </Style>

    <!-- Header Text Style -->
    <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
    </Style>

    <!-- Subtitle Text Style -->
    <Style x:Key="SubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Opacity" Value="0.7"/>
        <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
    </Style>

    <!-- Modern TextBox Style -->
    <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="12,10"/>
    </Style>

    <!-- Action Button in Grid -->
    <Style x:Key="ActionButtonStyle" TargetType="Button">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- Animated StackPanel ItemsPanel Template -->
    <ItemsPanelTemplate x:Key="AnimatedStackPanelTemplate">
        <StackPanel Orientation="Vertical" Spacing="8">
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True"/>
                    <RepositionThemeTransition/>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>
        </StackPanel>
    </ItemsPanelTemplate>

</ResourceDictionary>
